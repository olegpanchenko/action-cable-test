import Controller, { inject as controller } from '@ember/controller';

export default Controller.extend({
  wsStatus: '',
  sms: null,
  init: function () {
    this._super();

  },
  actions: {

  }
});
